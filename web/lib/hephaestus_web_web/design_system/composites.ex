defmodule HephaestusWebWeb.DesignSystem.Composites do
  @moduledoc """
  Namespace boundary for product-level design-system compositions.

  Composite implementations live below `design_system/composites/`, render
  only through `HephaestusWebWeb.DesignSystem`, and never emit raw markup.
  """
end
