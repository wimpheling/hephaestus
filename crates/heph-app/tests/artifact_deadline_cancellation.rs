//! Opt-in production-router proof for artifact stream deadline propagation.
#![cfg(feature = "test-fixtures")]

#[path = "composition/artifact_deadline_cancellation.rs"]
mod artifact_deadline_cancellation;
