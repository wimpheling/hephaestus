//! Opt-in production-router proof for browser-session creation and revocation.
#![cfg(feature = "test-fixtures")]

#[path = "composition/browser_session_lifecycle.rs"]
mod browser_session_lifecycle;
