[package]
name = "runtime-authority-postgres"
description = "PostgreSQL adapter for runtime authorization snapshots and sessions"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[package.metadata.hephaestus]
layer = "adapter"
context = "authorization"
postgres_adapter = true
database_context = "authorization"

[dependencies]
async-trait.workspace = true
capability-domain = { path = "../capability-domain" }
run-domain = { path = "../run-domain" }
runtime-authority = { path = "../runtime-authority" }
sqlx.workspace = true
time.workspace = true
uuid.workspace = true

[lints]
workspace = true
