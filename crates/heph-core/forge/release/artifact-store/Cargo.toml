[package]
name = "release-artifact-store"
description = "Safe one-way import and immutable local storage for release artifacts"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[package.metadata.hephaestus]
layer = "adapter"
context = "release"

[dependencies]
libc.workspace = true
release-domain = { path = "../domain" }
sha2.workspace = true
thiserror.workspace = true
uuid.workspace = true

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
