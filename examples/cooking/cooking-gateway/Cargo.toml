[package]
name = "cooking-gateway"
version = "0.1.0"
edition = "2024"
publish = false

# Released example software has its own offline dependency graph and build.
[workspace]

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
pedantic = "deny"

[dependencies]
ciborium = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
